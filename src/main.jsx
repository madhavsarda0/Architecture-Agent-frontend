import React from "react";
import ReactDOM from "react-dom/client";
import ArchitectureAgent from "./ArchitectureAgent.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ArchitectureAgent />
  </React.StrictMode>
);
